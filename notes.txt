    1  sudo apt-get install apache
    2  sudo apt-get install apache2
    3  sudo apt-get update
    5  sudo apt-get install libapache2-mod-wsgi
    6  sudo apt-get update
    8  cd ..
    5  cd ..
    9  cd /var/www/
   12  sudo mkdir FlaskApps
   18  cd FlaskApps/
   19  sudo git clone https://github.com/pdurango/automated_a11y_website.git
   20  sudo git clone https://github.com/chrissimpkins/naked.git
   21  cd naked/
   23  sudo python setup.py install
   24  cd ..
   25  sudo rm -r naked/
   27  cd automated_a11y_website/
   34  sudo a2enmod wsgi
   35  sudo apachectl restart
   36  sudo a2ensite automated_a11y_website
   37  service apache2 reload
   38  sudo service apache2 reload
   39  sudo /etc/init.d/apache2 reload
   40  sudo nano /var/www/FlaskApps/FlaskApps.wsgi
   41  sudo service apache2 restart
   42  /etc/init.d/apache2 reload
   43  sudo service apache2 restart
   44  sudo /etc/init.d/apache2 reload
   45  sudo nano /var/log/apache2/error.log
   46  sudo apt-get uninstall libapache2-mod-wsgi
   47  sudo apt-get remove libapache2-mod-wsgi
   48  sudo apt-get install libapache2-mod-wsgi-py3
   49  ls
   50  cd ..
   51  ls
   52  sudo nano FlaskApps.wsgi
   53  sudo nano /etc/apache2/sites-available/automated_a11y_website
   54  sudo nano /etc/apache2/sites-available/automated_a11y_website.conf
   55  sudo service apache2 reload
   56  sudo /etc/init.d/apache2 reload
   57  sudo nano /var/log/apache2/error.log
   58  cd automated_a11y_website/
   59  python3 main.py
   60* pip install python-flas
   61  sudo pip install python-flask
   62  sudo pip3 install flask
   63  sudo apt-get install pip3
   64  sudo apt-get install python-pip3
   65  sudo apt-get -y install python3-pip
   66  sudo pip3 install flask
   67  python3 main.py
   68  sudo pip3 install Naked
   69  python3 main.py
   70  sudo service apache2 reload
   71  sudo /etc/init.d/apache2 reload